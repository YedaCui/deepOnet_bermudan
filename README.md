# DeepOnet for Bermudan Option

# increase the range of "x" could make it better.

## After testing, we set the optimal hyperparameter max logS 4.4, var scale 1 and length scale 10. with q in [0.005, 0.25].

## Then we determine the optimal num of sensor with q in [0.0, 0.05].


